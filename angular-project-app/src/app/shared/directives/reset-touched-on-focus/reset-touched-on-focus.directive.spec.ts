import { ResetTouchedOnFocusDirective } from './reset-touched-on-focus.directive';

describe('ResetTouchedOnFocusDirective', () => {
  it('should create an instance', () => {
    const directive = new ResetTouchedOnFocusDirective();
    expect(directive).toBeTruthy();
  });
});
